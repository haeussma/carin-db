{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Extractor",
            "type": "python",
            "request": "launch",
            "module": "backend.extractor",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}